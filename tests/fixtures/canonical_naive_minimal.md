# Synthetic canonical ingestion fixture

The fictional Aurora relay reports a nominal latency of 17 milliseconds.
Its equally fictional backup relay reports 29 milliseconds. This document is
only a control input for verifying that naive retrieval builds chunk
embeddings without invoking knowledge-graph entity extraction.
